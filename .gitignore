# colcon build artifacts
build/
install/
log/

# python
__pycache__/
*.pyc

# piper_description/meshes(STL 13개, 10MB)는 추적한다 — 없으면 robot_state_publisher가 죽는다.
# 105MB였던 ranger_base.dae는 Ranger 패키지와 함께 제거됐다(sim-final 태그에 보존).

# 내부 인수인계 문서 — 로컬에만 둔다(공개 레포에 올리지 않음)
HANDOFF_JETSON.md
